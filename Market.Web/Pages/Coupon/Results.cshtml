@model Market.Web.Pages.Coupon.IndexModel

@if (Model.Results is { } coupons)
{
    @foreach (var coupon in coupons)
    {
        <tr>
            <td class="text-start">@coupon.CouponId</td>
            <td class="text-start">@coupon.CouponCode</td>
            <td class="text-start">@coupon.CouponName</td>
            <td class="text-end">@Convert.ToDecimal(string.Format("{0:0.00}", coupon.CouponDisccountAmount))</td>
            <td class="text-end">@Convert.ToDecimal(string.Format("{0:0.00}", coupon.CouponMinAmmount))</td>
            <td class="text-end">@coupon.CouponStartDate.ToString("yyyy-MM-dd HH:mm:ss")</td>
            <td class="text-end">@coupon.CouponEndDate.ToString("yyyy-MM-dd HH:mm:ss")</td>
            <td class="text-center">
                <button hx-get
                        hx-page="Index"
                        hx-page-handler="UpdateModal"
                        hx-route-id=@coupon.CouponId
                        hx-target="#modal-container"
                        class="btn-sm border-0 bg-transparent"
                        style="font-size:1.1rem">
                    <img src="~/img/pen.svg" alt="pen" width="16" />
                </button>
            </td>

            @*       <td style="text-align:center">

    <a name="newsletter"
    hx-get="@Url.Page("./Index", "UpdateModal")"
    hx-trigger="click"
    hx-target="#modals-here"
    hx-swap="innerHTML"
    hx-timeout="60s" type="button">
    <img style="margin-inline:0.5em" src="~/img/pen.svg" alt="pen" style="height: 1.2rem; mar">
    </a>
    <a href="#"
    name="newsletter"
    hx-get="@Url.Page("Index", "CreateModal")"
    hx-trigger="click"
    hx-target="#modal-container"
    class="btn btn-outline-secondary mb-2"
    style="font-size:1.1rem">
    <img src="~/img/plus.svg" alt="plus" width="16" />
    New
    </a>
    <img style="margin-inline:0.5em" src="~/img/trash.svg" alt="trash" style="height: 1.2rem">
    <img style="margin-inline:0.5em" src="~/img/circle-info.svg" alt="trash" style="height: 1.2rem">
    </td> *@
        </tr>
    }
}
else
{
    <tr>
        <td colspan="8">No Results for "@Model.Query"</td>
    </tr>
}